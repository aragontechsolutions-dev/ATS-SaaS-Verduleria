# Variables del POS (Vite las inyecta en BUILD-TIME). En Vercel se cargan como
# Environment Variables del proyecto (Production/Preview).

# URL del backend en Render, incluyendo el prefijo /api.
VITE_API_URL="https://ats-verduleria-api.onrender.com/api"

# Deploy single-tenant del MVP (hasta que exista auth JWT): tenant y usuario
# con los que operará el POS. Se obtienen tras correr el seed en Supabase.
VITE_TENANT_ID=""
VITE_USER_ID=""
